"""Replay storage, K-step optimization, and the learning pipeline.

Import concrete types from ``training.replay``, ``training.trainer``, or
``training.pipeline``. Keeping this initializer lightweight prevents the
pipeline's workflow dependencies from becoming an import cycle.
"""
